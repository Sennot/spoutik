#include <Geode/Geode.hpp>

// Baconsistent is registered through Geode's $modify hooks in src/hooks.
// Keeping this translation unit intentionally empty avoids requiring a custom
// loader entry point.
